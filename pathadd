# ~/dotfiles/pathadd
#!/bin/zsh

currDir=$PWD
cd ~/dotfiles

echo "export PATH=\"$1:\$PATH\"" >> zshrc
echo "Added $1 to PATH"
git add .
git commit -m "Updated ZSHRC"
git push origin main

source ~/.zshrc

cd "$currDir"